# FMD OS — веб-редизайн

## Концепция

Веб-интерфейс переведён в визуальную систему **FMD OS**: приватный инструментальный dashboard для быстрого поиска и управления устройством. Основной язык интерфейса — плотная telemetry-композиция с графитовым фоном, signal-green акцентом, моноширинными координатами, карточками статуса и картой как главным рабочим пространством.

Редизайн выполнен поверх существующей архитектуры React/Vite и сохраняет текущий API, шифрование, 2FA, модальные окна, фотогалерею, Leaflet-карту и серверные команды.

## 20 новых функций

| № | Функция | Что делает |
|---:|---|---|
| 1 | Sidebar-навигация | Переключает обзор, историю и центр команд без перезагрузки. |
| 2 | Сворачиваемая боковая панель | Освобождает рабочее пространство и сохраняет icon-only режим. |
| 3 | Мобильная навигация | Нижняя панель и mobile drawer для телефонов и узких экранов. |
| 4 | Live-status устройства | Показывает Connected/Idle и время последнего сигнала. |
| 5 | Telemetry-статистика | Карточки батареи, точности, расстояния и количества точек. |
| 6 | Автообновление | Переключатель фонового обновления локаций каждые пять минут. |
| 7 | Быстрое обновление | Отдельная кнопка ручного обновления с loading-состоянием. |
| 8 | Быстрый Locate | Отправляет существующую серверную команду locate all из dashboard. |
| 9 | Быстрый Ring | Отправляет команду ring одной кнопкой. |
| 10 | Быстрая вспышка | Отправляет команду flash из панели быстрых действий. |
| 11 | Экспорт JSON | Скачивает расшифрованную локально историю точек в JSON. |
| 12 | Экспорт CSV | Скачивает историю точек в табличном CSV-формате. |
| 13 | Activity timeline | Отдельный экран полной истории перемещений. |
| 14 | Поиск по истории | Ищет по координатам, источнику и локализованной дате. |
| 15 | Фильтр периода | Фильтрует историю за 24 часа, 7 дней или всё время. |
| 16 | Избранные точки | Сохраняет важные точки локально и отмечает их звездой. |
| 17 | Копирование координат | Копирует lat/lon в clipboard с toast-подтверждением. |
| 18 | Открытие в OpenStreetMap | Передаёт текущую точку во внешнюю карту. |
| 19 | Поделиться точкой | Использует Web Share API или fallback через clipboard. |
| 20 | Журнал команд | Показывает успешные и неуспешные быстрые команды с временем. |

Дополнительно добавлены циклическое переключение темы, обновлённый экран входа с 2FA-веткой, privacy-страница в едином стиле, responsive layout, reduced-motion режим и русская локаль по умолчанию для новых пользователей.

## Изменённые файлы

Основные изменения находятся в `web/src/components/Home.tsx`, `web/src/components/LoginForm.tsx`, `web/src/components/Header.tsx`, `web/src/components/PrivacyContent.tsx`, `web/src/globals.css`, `web/index.html` и `web/src/lib/store.ts`. В `user/user_repository.go` добавлена совместимость старых вызовов `RequestAccess` с текущей 2FA-сигнатурой через необязательный variadic-параметр.

## Проверка

`pnpm run type-check`, `pnpm run build` и `go test ./...` проходят. Vite всё ещё сообщает предупреждения о legacy `theme-init.js`, Leaflet dynamic import и размере чанка — это не блокирует сборку. `pnpm run lint` в исходной конфигурации требует обновления совместимости между `typescript-eslint` и TypeScript 7.0.2; эта проблема не связана с кодом редизайна.

## Android-этап

После получения Android-проекта следует перенести ту же модель навигации: обзор устройства, история, центр команд, экспорт/шаринг и локальные избранные точки. Для Android желательно сохранить единые названия команд и цветовые токены FMD OS, а мобильную нижнюю навигацию веб-версии использовать как основу для Material-навигации приложения.
